/**
 * Bootstrap Module
 */

require([
	'modules/module'
], function (module) {
	'use strict';

	console.log(module);
	// ausschließliches Laden / require von Standalone Modulen, die sonst nirgendwo aufgerufen werden

});
